# Auto detect text files and perform LF normalization
* text=auto
# Windows batch scripts must use CRLF
*.bat text eol=crlf
# shell scripts must use LF
*.sh text eol=lf
# lock files must use LF
*.lock text eol=lf
# toml files must use LF
*.toml text eol=lf

*.html linguist-vendored
*.qmd linguist-detectable
